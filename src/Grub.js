class Grub {
	constructor() {
		this.age = 0;
		this.color = 'pink';
		this.food = 'jelly';
	}
	eat() { return 'Mmmmmmmmm jelly'}
};


// Tasks
	// ES6 style
	// add following features to the class
		// an age property that is set to 0
		// a color property that is set to pink
		// a food property that is set to jelly
		// an eat method that returns 'Mmmmmmmmm jelly'

// what is ES6 style?
	// declration of properties and methos
		//write the following in 'class
			// property
				// constructor() { this.prop1 = val1; }
			// method
				// (method name) () { (functionality)}
	// subclass (class extension)
		// class (subclass) extends (superclass)
		//	constructor(val1) {
		// 		super(val1) // inherit all properties and methods
		//		this.val1 or this val2 // overwrites or add new properties
		// }

